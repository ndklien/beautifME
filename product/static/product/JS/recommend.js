/* function submitQueryFilter(queryList) {
    loc = document.getElementById('FilterResult');

    list = document.createElement('ul');

    queryLength = length(queryList);
    console.log('queryLength');
    for (i=0; i < queryLength; i++) {
        list_comp = document.createElement('li');
        list_comp.appendChild(queryList[i]);
        list.appendChild(list_comp);
    }
    loc.appendChild(list);
} */